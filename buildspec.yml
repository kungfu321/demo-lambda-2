version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g aws-cdk
      - npm install -g sst
      - npm install
  build:
    commands:
      - echo "AWS Account: $(aws sts get-caller-identity)"
      - sst deploy --stage prod
